Aims to be a lightweight command line based bus tracker. 
To use compile and run the executable with your API key (request one here: https://register.developer.obanyc.com )
Syntax would be ./main <API-KEY>
